<div class="min-h-screen bg-gray-100 flex items-center justify-center">
  <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
    <h2 class="text-2xl font-bold text-center mb-6">ログイン</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-4" }) do |f| %>
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>

      <div class="field flex flex-col">
        <%= f.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "mt-1 px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm" %>
      </div>

      <div class="field flex flex-col">
        <%= f.label :password, "パスワード", class: "block text-sm font-medium text-gray-700" %><br />
        <%= f.password_field :password, autocomplete: "current-password", class: "mt-1 px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 block w-full sm:text-sm" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field flex items-center justify-between">
          <div class="flex items-center">
            <%= f.check_box :remember_me, class: "h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded" %>
            <%= f.label :remember_me, "ログイン状態を保持する", class: "ml-2 block text-sm text-gray-900" %>
          </div>
          <%= link_to "パスワードをお忘れですか？", "#", class: "text-sm text-blue-500 hover:underline" %> 
        </div> 
      <% end %>

      <div class="actions" id="login_button_id">
        <%= f.submit "ログイン", class: "w-full py-2 px-4 bg-secondary text-gray-700 font-bold rounded-md hover:bg-emerald-200 focus:outline-none focus:ring-2 focus:ring-indigo-500", id: "no-double_click-button" %>
      </div>
    <% end %>

    <p class="mt-4 text-sm text-center text-gray-500">
      <%= link_to "アカウントをお持ちでない方はこちら", new_user_registration_path, class: "text-blue-500 hover:underline" %>
    </p>
  </div>
</div>

<%= render "devise/shared/links" %>
